{% extends 'base.html.twig' %}

{% block title %}
    {{ post.title }}
{% endblock %}

{% block body %}
    <h2>{{ post.title }}</h2>
    <em>{{ post.category }}</em>
    <hr>
    <img src="{{ '/uploads/' ~ post.image }}" alt="{{ post.title }}">
    <p>The description of the post wil be shown here</p>
{% endblock %}
